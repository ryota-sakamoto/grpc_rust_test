syntax = "proto3";

package sample;

service Greeter {
    rpc hello(HelloRequest) returns (HelloResponse) {};
}

message HelloRequest {
    string name = 1;
}

message HelloResponse {
    string message = 1;
}